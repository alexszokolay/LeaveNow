A real-time transit reliability planner that tells you when to leave and how confident that ETA really is.

## Setup
- Create a `.env` file with `TRANSIT_API_KEY=your_key`.
- Optional: set `TRANSIT_REFRESH_TOKEN` to protect the cache refresh endpoint.
- Optional: set `MAPBOX_TOKEN` to enable high-quality route geometry and basemap styling.

## Development
- `npm install`
- `npm run dev`
